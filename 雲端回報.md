# 雲端回報：README 跨月連上天數的矛盾

分支：`cloud/readme-cross-month`（從 main `ca83bcf` 開出來，沒有動 main，也沒有開 PR）

## 問題

README 規則第 5 條說連上天數「跨月也接著算」，但「還沒做」那段又說跨月的連續天數還沒接起來，兩處說法互相矛盾。

## 實際行為（依據 index.html）

兩處說法都只對一半，要分成兩個部分看：

### 1. 自動排班：有接跨月

- `leadRun(days, sk)`，`index.html:553-568`
  - 從這個月 1 號的前一天開始，往回數同一個人在同一班別（`sk`）連上了幾天，最多看 12 天（`while(n < 12)`，第 561 行）。
- `index.html:636`：`meta.lead = leadRun(days, sk)`
- `poolCost()`，`index.html:586-596`
  - 第 588 行：每個人的 `run` 從 `meta.lead[p]` 開始，不是從 0 開始。
  - 第 592 行：超過 `RUN_SOFT`（3 天，第 469 行）每天成本加 220。
  - 第 593 行：超過 `RUN_HARD`（6 天，第 470 行）每天成本加 4000。
- 結論：排班時，「一次連上不超過 3 天」和「連上不超過 6 天」這兩條都有把上個月月底的班接起來算。

### 2. 統計表和警告：沒有接跨月

- `computeStats()`，`index.html:1022-1067`
  - 第 1025 行：`run` 初始化成 0。
  - 第 1064-1065 行：只逐日數這個月，沒有讀上個月的資料。
- 受影響的地方：
  - 統計表「連上」欄的橘色／紅色標記（第 1114 行）
  - 「連續上 N 天（七休一上限 6）」警告（第 987 行）
  - 「有人連上超過 3 天」警告（第 1000-1007 行）
- 同一個函式裡的單週上限有跨月算（第 1027-1044 行），兩者做法不一樣。

## README 改了哪裡

1. 規則第 5 條：改寫成「自動排班時」跨月接著算，註明第 3 條的 6 天也一樣，點出 `leadRun()` 和 `poolCost()`，並說明統計表和警告還沒接跨月。
2. 「還沒做 / 待決定」第一條：從「跨月的連續上班天數沒有接起來算」改成只有統計表和警告的「最長連上」沒接跨月，並說明原因是 `computeStats()` 每個月都從 0 開始數。

`index.html` 沒有改。

## 不確定的地方

- 以上是讀程式碼得出的結論，沒有實際在瀏覽器跑過跨月的案例，也沒有跑 `測試-收斂.js`、`測試-完整.js`。
- `leadRun()` 只看同一個班別。`poolOf()`（第 507 行）是依 `p.shift` 分組，一個人只會在一個班別，所以目前不影響結果。但如果以後有人改班別，上個月在另一班別的連上天數就不會算進來。
- `leadRun()` 需要上個月已經有排班資料存在 `DB.months` 裡；上個月沒排過班的話，就沒有東西可以接。
